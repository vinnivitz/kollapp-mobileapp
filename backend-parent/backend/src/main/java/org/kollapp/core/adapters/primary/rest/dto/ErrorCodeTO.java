package org.kollapp.core.adapters.primary.rest.dto;

/**
 * Error codes for specific error handling on client side.
 * These codes allow the client to handle errors in a custom way
 * instead of showing a generic error alert.
 */
public enum ErrorCodeTO {
    /**
     * The user's email is not confirmed yet.
     */
    EMAIL_NOT_CONFIRMED
}
